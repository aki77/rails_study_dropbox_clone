= simple_form_for(folder_form_args(@folder)) do |f|
  = f.error_notification
  = f.error :base

  .form-inputs
    - case type
    - when :new, :edit
      = f.input :name
    - when :move
      = render 'user_items/input_parent', form: f, item: @folder

  .form-actions
    = f.button :submit
